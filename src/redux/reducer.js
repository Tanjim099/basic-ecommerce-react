const initialState = {
    auth: false,
    product: [],
    cartItem: []
}


function reducer(oldState = initialState, { type = "", payload = "" }) {
    switch (type) {
        case "":
            return
        default:
            return
    }
}

export default reducer